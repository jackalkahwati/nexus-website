{
  "name": "@nexus/database",
  "version": "0.1.0",
  "description": "Shared database models and utilities for Nexus applications",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "lint": "eslint src/**/*.ts",
    "test": "jest",
    "generate": "prisma generate"
  },
  "dependencies": {
    "@prisma/client": "^6.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.5.0",
    "prisma": "^6.0.0",
    "typescript": "^5.1.6"
  }
}